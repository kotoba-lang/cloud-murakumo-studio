;; kotoba.lang.http.host.httpkit/with-channel binds `channel` like `let`; clj-kondo
;; doesn't know that shape without this hint.
{:linters {:unresolved-symbol {:exclude [(kotoba.lang.http.host.httpkit/with-channel [channel])]}}}
